import React, { ReactElement } from "react";
import TLayout from "../../components/layout/TLayout";

interface Props {}

export default function score({}: Props): ReactElement {
  return <TLayout>Studet Score</TLayout>;
}
