import React, { ReactElement } from "react";
import Layout from "../components/layout/layout";

interface Props {}

export default function Announcement({}: Props): ReactElement {
  return <Layout>Anouncement</Layout>;
}
